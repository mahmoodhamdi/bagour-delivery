name: customer_app
description: "Bagour Delivery - Customer App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # State Management
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5

  # Networking
  dio: ^5.4.0

  # Local Storage
  shared_preferences: ^2.2.2
  flutter_secure_storage: ^9.0.0

  # UI Components
  cupertino_icons: ^1.0.8
  flutter_svg: ^2.0.9
  cached_network_image: ^3.3.1
  shimmer: ^3.0.0
  flutter_spinkit: ^5.2.0

  # Forms & Validation
  flutter_form_builder: ^9.2.1
  form_builder_validators: ^11.1.2

  # Navigation
  go_router: ^13.2.0

  # Maps & Location (FREE - OpenStreetMap)
  flutter_map: ^6.1.0
  latlong2: ^0.9.1
  geolocator: ^11.0.0
  http: ^1.2.0
  # google_maps_flutter: ^2.5.3  # Commented out - using free OSM instead
  # geocoding: ^2.2.0  # Commented out - using Nominatim instead

  # Firebase
  firebase_core: ^2.27.0
  firebase_auth: ^4.17.8
  firebase_messaging: ^14.7.19

  # Social Auth
  google_sign_in: ^6.2.1

  # JSON Serialization
  freezed_annotation: ^2.4.1
  json_annotation: ^4.9.0

  # Utilities
  intl: any
  url_launcher: ^6.2.4
  package_info_plus: ^5.0.1
  connectivity_plus: ^5.0.2
  permission_handler: ^11.3.0
  image_picker: ^1.0.7

  # Real-time
  socket_io_client: ^2.0.3+1

  # Notifications
  flutter_local_notifications: ^17.0.0

  # Animations
  lottie: ^3.1.0

  # Rating
  flutter_rating_bar: ^4.0.1

  # Carousel
  carousel_slider: ^4.2.1

  # WebView
  webview_flutter: ^4.7.0

  # Photo viewer
  photo_view: ^0.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  riverpod_generator: ^2.4.0
  build_runner: ^2.4.8
  json_serializable: ^6.7.1
  freezed: ^2.5.2

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
    - assets/animations/

  fonts:
    - family: Cairo
      fonts:
        - asset: assets/fonts/Cairo-Regular.ttf
        - asset: assets/fonts/Cairo-Bold.ttf
          weight: 700
        - asset: assets/fonts/Cairo-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Cairo-Medium.ttf
          weight: 500
        - asset: assets/fonts/Cairo-Light.ttf
          weight: 300
